actor display {
    var var2: num = 1.5;
    when start {
	displayAsText();
	displayStr("amogus");
	repeat 25 { 
	    waitForProcess();
	    displayStr(intToString(randomDigit()));
	    wait(0.25);
	    displayStr("");
	}
    }	
}
